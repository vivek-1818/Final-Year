<Page x:Class="DeezFiles.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:DeezFiles.Models"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" 
      Background="#212121"
      Title="MainPage"
      Height="800" Width="1280">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="DFTarget" Text="DEEZFILES" FontFamily="Segoe UI" FontSize="36" Foreground="White" FontWeight="Bold" Margin="16,16,16,10" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" RenderTransformOrigin="0.499,-0.476" Height="23" Width="1010">
                <Border Height="1" Background="#2c2c2c" HorizontalAlignment="Center" Width="800"/>
                <Button x:Name="LogOut" Click="LogOut_Click" Background="Transparent" Padding="0" Margin="16,0,0,0" Width="92" BorderThickness="0,0,0,0">
                    <StackPanel Orientation="Horizontal" Cursor="Hand">
                        <TextBlock Text="Logout" FontSize="12" Foreground="#585858"/>
                        <Image Source="pack://application:,,,/Assets/logout.png" Height="16" Width="16" Stretch="Fill" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>

        <StackPanel Margin="12,112,12,12" Grid.RowSpan="2">
            <TextBlock x:Name="UserID" Text="VaderPooperFader66" FontSize="18" FontFamily="Roboto" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="White"/>
            <TextBlock x:Name="UserAdd" Text="@vndjnsknsdn" FontSize="12" Foreground="#585858" HorizontalAlignment="Center"/>

            <Grid Margin="24" Background="#252525" Height="320" Width="831">
                <Grid.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="30"/>
                    </Style>
                </Grid.Resources>
                <DockPanel Margin="10">

                    <!-- Headers -->
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="File Name"   Grid.Column="0" FontWeight="Bold" Foreground="LightGray" Grid.ColumnSpan="2" Margin="31,0,179,0"/>
                        <TextBlock Text="Date"        Grid.Column="1" FontWeight="Bold" Foreground="LightGray" Grid.ColumnSpan="2" Margin="31,0,129,0" TextAlignment="Center"/>
                        <TextBlock Text="Size"        Grid.Column="2" FontWeight="Bold" Foreground="LightGray" Margin="0,0,10,0" TextAlignment="Center"/>
                        <TextBlock Text="Download"    Grid.Column="3" FontWeight="Bold" Foreground="LightGray" Margin="0 0 10 0" TextAlignment="Center"/>
                    </Grid>

                    <!-- Scrollable rows container -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Bottom">
                        <StackPanel x:Name="RowsPanel" Orientation="Vertical" />
                    </ScrollViewer>

                </DockPanel>
                <TextBlock Name="TotalFilesCount" Text="Total Files: 0" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#535353" Margin="10" VerticalAlignment="Bottom"/>
            </Grid>

            <Grid Width="600" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" Margin="16,0">
                        <TextBlock Text="Total Files" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Text="Uploaded" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Name="UploadSizeTotal" Text="0KB" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#7e7e7e" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Button x:Name="Upload" Click="Upload_Click" FontSize="16" FontFamily="Segoe UI" FontWeight="Bold" Background="#c9a9ff" Foreground="#2a2a2a" Margin="16,0" Width="104" Height="59" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="15" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Upload" FontSize="14" Foreground="#585858"/>
                            <Image Source="pack://application:,,,/Assets/Upload.png" Height="18" Width="18" Stretch="Fill" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <StackPanel HorizontalAlignment="Center" Margin="16,0">
                        <TextBlock Text="Total Storage" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="DarkGray" HorizontalAlignment="Center"/>
                        <TextBlock Text="In Use" FontSize="12" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="#535353" HorizontalAlignment="Center"/>
                        <TextBlock Text="1.6GB" FontSize="12" FontFamily="Segoe UI" FontWeight="Bold" Foreground="#7e7e7e" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>